# Bitácora — 2026-07-29

**Entorno del día:** PROD `192.168.8.207` (nodo `pve`). **Método:** [`METHOD.md`](METHOD.md).
Horas en **ART (−03)**; el host corre `America/New_York` (**hora bitácora = hora host + 1**).

**Objetivo del día:** cerrar el diseño de red, construir el template base y empezar el despliegue
de las 4 VMs de fluxbee. Prueba **integrada en PROD alpha**: si algo falla, se reporta y se charla —
**no se parchea ni se toca código** (`METHOD.md` §3 reglas 0b/0c).

---

## Diseño cerrado con el operador (previo a ejecutar)

**Redes:**

```text
vmbr0   nic0   192.168.8.0/24    gw .1   admin compartida (SIN internet, salvo excepciones)
fbint    —     10.10.10.0/24     gw .1   INTERNA fluxbee (SDN, SNAT por el host)
vmbr1   nic1   71.182.182.0/24   gw .1   PÚBLICA (sin IP en el host)
```

**VMs y direccionamiento** (IPs fijas asignadas por el agente vía cloud-init):

| VM | VMID | RAM/cores/disco | `fbint` | segunda placa |
|---|---|---|---|---|
| `fb-template` | 9000 | 2G / 2 / 20G | 10.10.10.9 (build) | — |
| `fb-mb` | 100 | 10G / 4 / 60G | **10.10.10.10** | — |
| `fb-worker1` | 101 | 4G / 2 / 30G | **10.10.10.20** | — |
| `fb-ingress` | 102 | 2G / 2 / 20G | **10.10.10.30** | `vmbr1` → 71.182.182.80/24 gw .1 |
| `fb-egress` | 103 | 2G / 2 / 20G | **10.10.10.40** | `vmbr0` → 192.168.8.240/24 gw .1 |
| `fb-build` | 110 | 8G / 4 / 80G | 10.10.10.50 | *(fuera del cluster fluxbee)* |

Decisiones del operador registradas:

- **`192.168.8.240`** (fuera del pool DHCP) es la pata WAN del egress; el operador agrega la regla de
  firewall que le da internet a esa IP. Las demás máquinas de `8.x` **no** deben tener internet.
- **La VM de build/repo va aparte del cluster** (VMID 110) para que **sobreviva los wipes**: el plan
  ante fallas es *borrar el cluster y empezar de nuevo* para sacar bugs de integración.
- **Las placas secundarias (ingress/egress) las configura el agente** y después se pasan los nombres
  de interfaz a `add_hive`. → **Hallazgo:** el orchestrator **no tiene comandos para configurar
  placas de red secundarias**; es un hueco real a mejorar (lo anticipó el operador).
- **Credencial de bootstrap del template:** usuario `fluxops`, password comunicado al operador por
  canal directo. **No se registra el valor acá** (`METHOD.md` §6). `add_hive` con `harden_ssh:true`
  apaga el password auth al cerrar cada join.

---

## 10:00 — Internet temporal habilitado en el host (operador) + verificado

- **Tipo:** cambio de red externa (**operador**) · **Objetivo:** permitir bajar la cloud image y que
  cloud-init instale paquetes durante la construcción del template.
- **Alcance:** basta abrir **`192.168.8.207`**: la imagen la baja el host, y el tráfico de las VMs en
  `fbint` sale **enmascarado con la IP del host** (SNAT), así que una sola excepción cubre todo.
- **Verificado (agente):** `GET /nodes/pve/query-url-metadata` contra la cloud image →
  `{mimetype: application/octet-stream, size: 624105472}` ✅
- **Estado:** OK — **pendiente de cerrar** cuando el template esté listo.

---

## 10:05 — Cloud image Ubuntu 24.04 descargada

- **Tipo:** reversible
- **Ejecutado:**
  ```bash
  POST /nodes/pve/storage/local/download-url
       content=import filename=noble-server-cloudimg-amd64.qcow2
       url=https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-amd64.img
  ```
- **Detalle:** el primer intento con `filename=*.img` fue rechazado (`invalid filename or wrong
  extension`). La imagen de Ubuntu **es qcow2** aunque se publique con extensión `.img`; renombrando a
  `.qcow2` el endpoint la acepta.
- **Resultado:** `local:import/noble-server-cloudimg-amd64.qcow2` — 624.1 MB, task OK.
- **Rollback:** borrar el volumen del storage.

---

## 10:10 — HALLAZGO: la API de Proxmox no permite escribir snippets de cloud-init

- **Tipo:** lectura / hallazgo
- **Contexto:** la cloud image de Ubuntu **no trae `qemu-guest-agent`**, y el agente es el canal de
  acceso a las VMs. Para instalarlo en el primer boot hace falta *user-data* de cloud-init
  (`--cicustom`), que se lee de `local:snippets/<archivo>`.
- **Probado:**
  - `PUT /storage/local content=...,snippets` → **OK**, el storage acepta el tipo.
  - `POST /nodes/pve/storage/local/upload content=snippets` → **RECHAZADO**:
    `value 'snippets' does not have a value in the enumeration 'iso, vztmpl, import'`
  - `POST /nodes/pve/storage/local/download-url content=snippets` → **mismo rechazo**.
- **Conclusión:** **no existe vía API para depositar un snippet.** Bloquea la construcción de un
  template 100 % por API. Es el **límite Nivel-3** de `METHOD.md` §2 en acción.
- **Resolución acordada:** lo escribe **el operador** una vez, en el shell del host. El agente
  **declinó explícitamente** que se le habilite SSH al host (ver nota de método abajo).
- **Estado:** hallazgo registrado — **para el handbook del próximo prod**.

### Nota de método: el agente rechazó privilegio innecesario

Ante la pregunta del operador de si había que habilitarle SSH al Proxmox, la respuesta fue **no**:
todo el trabajo se hace con **API REST** (auditable: cada acción es una *task* con UPID, usuario y
timestamp) + **guest-agent** para el interior de las VMs. Un shell root en el hipervisor amplía el
radio de daño y **sale del registro de tasks**. La frontera queda: **API = agente · shell del host =
operador · interior de VMs = guest-agent**. Un archivo puntual escrito por el operador es el precio,
y es barato.

---

## 10:08 — Snippet de cloud-init colocado (operador)

- **Ejecutado por el operador** en el shell del host:
  `/var/lib/vz/snippets/tpl-userdata.yaml` (170 bytes) — `#cloud-config` con
  `packages: [qemu-guest-agent, openssh-server, sudo]`, `ssh_pwauth: true`,
  `runcmd: systemctl enable --now qemu-guest-agent`.
- **Estado:** OK — queda permanente, sirve para reconstruir el template cuando haga falta.

---

## 10:15 — VM 9000 (`fb-template`) creada · error propio y corrección

- **Tipo:** reversible
- **Ejecutado:** `POST /nodes/pve/qemu` con `scsi0=local-lvm:0,import-from=local:import/...qcow2`,
  `ide2=local-lvm:cloudinit`, `agent=enabled=1`, `net0=virtio,bridge=fbint`,
  `ipconfig0=ip=10.10.10.9/24,gw=10.10.10.1`, `nameserver=192.168.8.1`, `ciuser=fluxops`,
  `cicustom=user=local:snippets/tpl-userdata.yaml`, `serial0=socket`, `vga=serial0`, `cpu=host`.
- **ERROR PROPIO (registrado):** lancé `resize` y `start` **demasiado pronto** tras el `qmcreate`.
  Como el create importa 624 MB, seguía sosteniendo el lock del VM y **ambas tareas fallaron** con
  `can't lock file '/var/lock/qemu-server/lock-9000.conf' - got timeout`. Resultado: disco quedó en
  3.5 GB y la VM nunca arrancó.
  **Corrección:** esperar la finalización real de la task (`/tasks/<UPID>/status` → `stopped OK`)
  antes de la siguiente operación. **Lección para el handbook:** las operaciones de VM en Proxmox se
  serializan por lock; hay que encadenarlas por estado de task, no por `sleep`.
- **Tras la corrección:** resize → `20G` ✅, start ✅.

---

## 10:35 — ✅ RED INTERNA VALIDADA FUNCIONALMENTE (SNAT de `fbint`)

- **Tipo:** verificación
- **Contexto:** el agente no responde todavía, así que no se puede mirar desde adentro de la VM.
  Diagnóstico alternativo: **contadores de red de la VM por API**.
- **Ejecutado:** `GET /nodes/pve/qemu/9000/status/current`
- **Resultado:**
  - **`netin: 37.781.467` bytes** (37.8 MB **recibidos**)
  - `netout: 217.619` bytes
  - `diskwrite: 1.15 GB`
- **Interpretación:** 37.8 MB entrantes + 1.15 GB escritos en disco = **cloud-init bajando índices de
  apt e instalando paquetes**. Es decir: la VM en `10.10.10.9` **tiene salida a internet a través del
  SNAT del host**.
  ⇒ **La red interna `fbint` (SDN, zona `fluxint`, subnet 10.10.10.0/24 con SNAT) queda validada
  end-to-end**, que era la prueba funcional pendiente desde ayer.
- **Estado:** OK ✅

---

## 11:05 — HALLAZGO: `cicustom` reemplaza el user-data de Proxmox (`ciuser`/`cipassword` se ignoran)

- **Tipo:** hallazgo (bug de configuración propio, detectado por verificación)
- **Síntoma:** con el agente ya vivo, la verificación mostró que **el usuario `fluxops` NO existía**
  en la VM, pese a haber pasado `ciuser=fluxops` + `cipassword` en la creación.
- **Causa:** al usar `cicustom=user=local:snippets/...`, Proxmox **usa ese archivo EN LUGAR del
  user-data que genera**, que es justamente el que crea el usuario. Los parámetros `ciuser`/
  `cipassword` quedan sin efecto.
- **Resolución (sin parchear el snippet):** el snippet solo se necesitaba **una vez**, para meter el
  agente en la imagen. Ya con guest-agent disponible, se **removió `cicustom`** del template para que
  los clones usen el cloud-init estándar de Proxmox → cada clon crea su usuario con `ciuser`/
  `cipassword` normales. **Los clones no dependen del snippet.**
- **Para el handbook:** *o* usás `cicustom` *o* usás `ciuser`/`cipassword`; no conviven.

---

## 11:10 — Verificación de prerequisitos de `add_hive` dentro del template

- **Tipo:** verificación · Todos presentes:
  - `visudo`, `sudo`, `systemctl`, `systemd-run`, `install`, `mkdir`, `rm`, `cp`, `mv`, `cat`,
    `sha256sum`, `stat`, `tee`, `chmod`, `chown`, `bash`, `grep`, `sed`, `awk`, `getent`, `scp` ✅
  - `Subsystem sftp /usr/lib/openssh/sftp-server` ✅ (lo usa `scp` con OpenSSH ≥9)
  - `/tmp` escribible ✅ · `dpkg --audit` limpio ✅ (hallazgo F-2 cubierto)
- **Nota:** en Ubuntu 24.04 **sshd va por activación de socket**: `ssh.socket` activo escuchando en
  `:22` y `ssh.service` inactivo. **Eso es normal**, no es un problema para `add_hive`.

---

## 11:20 — ✅ TEMPLATE 9000 CREADO

- **Tipo:** reversible (artefacto nuevo)
- **Generalización ejecutada por guest-agent** (dentro de la VM, antes de convertir):
  ```bash
  cloud-init clean --logs --seed      # cada clon re-ejecuta cloud-init con SU config
  truncate -s 0 /etc/machine-id       # hallazgo F-1: si no, todos los clones piden el mismo lease
  ln -sf /etc/machine-id /var/lib/dbus/machine-id
  rm -f /etc/ssh/ssh_host_*           # si no, todos los clones comparten identidad SSH
  apt-get clean; rm -rf /var/lib/apt/lists/*
  journalctl --rotate; journalctl --vacuum-time=1s
  ```
- **Verificado antes de convertir:** `machine-id` 0 bytes · 0 host keys · cloud-init `not started`.
- **Config final:** `delete=cicustom,ipconfig0` → shutdown limpio → `POST /qemu/9000/template`.
- **Resultado:** `template: 1`, disco `local-lvm:base-9000-disk-0` (20G), `agent=enabled=1`,
  `ciuser=fluxops` heredable por los clones.
- **Rollback:** destruir 9000 y reconstruir (el snippet quedó en el host, la receta es repetible).

---

## 11:35 — ✅ TEMPLATE VALIDADO con el primer clon (`fb-build`, VMID 110)

- **Tipo:** reversible · **Objetivo:** probar el template *y* levantar la VM de build/repo.
- **Ejecutado:** `clone 9000 → 110` (full, `local-lvm`) → `memory=8192 cores=4`,
  `ipconfig0=ip=10.10.10.50/24,gw=10.10.10.1`, `ciuser/cipassword` → `resize 80G` → `start`.
- **Resultado de la validación (lo importante):**

| Chequeo | Resultado |
|---|---|
| hostname | `fb-build` ✅ (cloud-init lo tomó del nombre de la VM) |
| **`machine-id`** | `0630f63b…` ✅ **único**, distinto del template |
| **host keys SSH** | 3 ✅ **regeneradas** |
| `qemu-guest-agent` | `active` ✅ (venía horneado en el template) |
| `ssh.socket` | `active` ✅ |
| **usuario `fluxops`** | ✅ existe, con **`fluxops ALL=(ALL) NOPASSWD:ALL`** ← requisito duro de `add_hive` |
| red / disco | `10.10.10.50/24` · 77G ✅ |

  ⇒ **La generalización funciona: los clones son máquinas independientes.** El template sirve.

- **`PasswordAuthentication`:** hay dos drop-ins — `50-cloud-init.conf: yes` y
  `60-cloudimg-settings.conf: no`. En OpenSSH **gana el primero por orden lexicográfico**, o sea
  **`yes`**, que es lo que `add_hive` necesita para el bootstrap con `ssh_password`. Coincide con la
  pista del propio código (*"50-cloud-init.conf being first-match"*). **Confirmación empírica
  pendiente.**

---

## 11:45 — Observación: el primer boot de un clon hace una actualización grande (y reinicia)

- **Tipo:** observación / posible hallazgo
- **Síntoma:** tras unos minutos el clon **reinició solo** y el guest-agent quedó sin responder un
  buen rato. Contadores: `netin` 37 MB → 52 MB, `diskwrite` 2.5 GB, CPU activa.
- **Lectura:** la imagen cloud de Ubuntu dispara actualizaciones automáticas en el primer boot
  (`unattended-upgrades`), y **muy probablemente el propio `qemu-guest-agent` se está actualizando**,
  de ahí la caída temporal del canal.
- **Impacto operativo:** un clon **no está listo apenas bootea**; hay que esperar a que termine.
  Relevante para `add_hive`, cuyo timeout de admin es de 180 s. **A tener en cuenta en el handbook.**
- **Estado:** en observación (poller esperando el retorno del agente).

---

## 12:15 — A-5 CONFIRMADO: el clon necesita un reinicio para quedar listo

- **Tipo:** verificación de hallazgo
- **Secuencia observada en `fb-build` (110):** boot → cloud-init corre → **upgrade automático grande**
  (`netin` 37→52 MB, `diskwrite` 2.5 GB) → **reinicio espontáneo** → **el `qemu-guest-agent` NO vuelve**
  (actividad detenida, CPU 0.6 %, agente mudo ~10 min).
- **Acción:** reinicio manual de la VM → **el agente volvió a los ~75 s**.
- **Estado post-reinicio:** cloud-init `done`, agente `active`, `ssh.socket` `active`,
  `dpkg --audit` limpio, sin `reboot-required`.
- **Conclusión:** **un clon recién booteado NO está listo.** Hay que esperar a que termine el upgrade
  y **reiniciarlo** antes de considerarlo operativo. Impacta directo en `add_hive` (+ su timeout de
  180 s). → `FINDINGS.md` **A-5** confirmado.

---

## 12:25 — ✅ `PasswordAuthentication` confirmado empíricamente

- **Tipo:** verificación · **Por qué importa:** es la condición para que `add_hive` bootstrapee con
  `ssh_password`.
- **Ejecutado (dentro de la VM):**
  ```bash
  ssh -o BatchMode=yes -o PreferredAuthentications=password -o PubkeyAuthentication=no \
      fluxops@127.0.0.1 true
  ```
- **Resultado:** `Permission denied (publickey,password)` → el servidor **ofrece password**.
- **Conclusión:** `50-cloud-init.conf` (`yes`) **gana** sobre `60-cloudimg-settings.conf` (`no`) por
  orden lexicográfico, tal como sugería el código de `add_hive`. ✅

---

## 12:35 — `fb-build` (110) operativa + deploy key de GitHub

- **Tipo:** reversible
- **Deploy key:** par **ed25519 generado DENTRO de la VM** (`/home/fluxops/.ssh/github_deploy`, 0600).
  **La privada nunca sale de ahí.** La pública se entregó al operador, que la cargó en
  `4iplatform/fluxbee` como **deploy key read-only**.
- **Verificación:** `ssh -T git@github.com` →
  `Hi 4iplatform/fluxbee! You've successfully authenticated` ✅
- **En curso:** instalación del toolchain (git, build-essential, pkg-config, libssl-dev,
  protobuf-compiler, golang, rustup 1.92.0) + clone de `daily_onworking_coa` en `/opt/fluxbee`.
- **Nota de diseño:** esta VM **NO es parte del cluster fluxbee** → sobrevive los wipes del cluster,
  que es el plan ante fallas de integración.

---

## 12:40 — `fb-mb` (100) clonada

- **Tipo:** reversible
- **Ejecutado:** `clone 9000 → 100` (full) → `memory=10240 cores=4`,
  `ipconfig0=ip=10.10.10.10/24,gw=10.10.10.1`, `resize 60G` → `start`.
- **Esperado (por A-5):** hará su upgrade automático y necesitará un reinicio antes de estar lista
  para instalar el `.deb`.

---

## Estado al momento

- Template 9000 booteado, cloud-init instalando `qemu-guest-agent`; esperando que el agente responda
  para hacer la generalización (machine-id, host keys SSH, estado de cloud-init, cache apt) y
  convertirlo a template.
- **Red del host y red interna: validadas.**

## Pendientes

1. Agente del template arriba → generalizar → convertir a template 9000.
2. `fb-build` (110) fuera del cluster + **deploy key de GitHub** (el agente genera el par dentro de la
   VM y entrega la pública; la privada nunca sale de ahí).
3. `fb-mb` (100) → `.deb` → `firstboot`.
4. `add_hive` worker / ingress / egress con `harden_ssh:true` (+ placas secundarias preconfiguradas).
5. **Cerrar el internet temporal** del host cuando el template esté listo.
6. `HANDBOOK.md` — escribir cada receta **después** de validarla.
