# 2026-09-25 — repo apt de PROD accesible para los devs (placa 8.x en fb-build)

> Pedido del operador: *"darle acceso a la máquina del repo de fluxbee para que los demás dev puedan
> instalárselo desde la infra interna"* — (1) confirmar que un `apt install` cualquiera instala
> fluxbee y (2) darle a la máquina del repo una pata en la 8.x (DHCP). Los devs trabajan en la 4.x,
> que según el operador llega a la 8.x por VPN interna.

## Resultado

- **Repo accesible desde la infra interna:** `deb [trusted=yes] http://192.168.8.180:8900 ./`
  (fb-build ahora tiene `ens19` en `vmbr0`, DHCP). Verificado desde la Mac del operador por VPN
  (origen `10.100.0.2`, fuera de la 8.x) y con `apt-get install -s fluxbee` desde un Ubuntu 24.04
  limpio en la 8.x (fb-egress): resuelve 13 paquetes (postgresql-16 del archive + fluxbee 0.1.33).
- **Repo persistente:** la unit `fluxbee-apt` ahora es un archivo habilitado (antes, transitoria:
  moría con cada reboot). Validado reiniciando fb-build: el repo volvió solo.
- **Publish sin ventana vacía:** el índice se regeneraba *in place* y quedaba en 0 paquetes ~5 min
  por publish; ahora se reemplaza con renames atómicos. Validado: 32 muestras durante un publish
  real, mínimo visto por el cliente = 33.
- **Causa raíz de la "flakiness del qga":** cualquier carácter por encima de U+00FF en un `exec`
  por la API de Proxmox deja el guest-agent trabado. Probado A/B. `lab/pve.py` corregido.

Hallazgos: [`FINDINGS.md`](FINDINGS.md) B-10, B-11, B-12.

---

## 14:40 — Relevamiento de fb-build (VM110)
- **Entorno:** prod (192.168.8.207)
- **Tipo:** lectura
- **Objetivo:** ver si la máquina del repo tiene pata en la 8.x y en qué estado está el repo.
- **Ejecutado:** `GET /nodes/pve/qemu/110/config`, `.../103/config` (par con `vmbr0`), firewall
  options; en el guest: netplan, `ip route`, `systemctl show fluxbee-apt`, `ufw status`.
- **Resultado:** **no tenía pata en la 8.x** (solo `net0` en `fbint`, 10.10.10.50). Ruta por
  defecto por `fbint` → 10.10.10.1 = la salida a internet del build (github 200): no hay que
  romperla, y la 8.x **no tiene internet**. Repo activo (33 versiones, 0.1.33) pero en una unit
  **transitoria** (`/run/systemd/transient/`). Ubuntu 24.04.4, glibc 2.39. Firewall PVE apagado,
  ufw inactivo. El `.deb` declara `libc6 (>= 2.39)` → solo instala en Ubuntu 24.04+ amd64.
- **Estado:** OK
- **Rollback:** N/A

## 14:44 — Snapshot de fb-build
- **Entorno:** prod · **Tipo:** reversible
- **Ejecutado:** `pve.py snapshot 110 pre-lan-nic-20260925`
- **Estado:** OK · **Rollback:** `pve.py rollback 110 pre-lan-nic-20260925`

## 14:45 — `pve.py push` no podía subir archivos con caracteres UTF-8 anchos
- **Entorno:** prod (herramienta del lab) · **Tipo:** lectura
- **Resultado:** `HTTP 500 Wide character in subroutine entry` (Perl de la API). Corregido en
  `lab/pve.py` (se codifican los bytes en base64 localmente y se manda `encode=0`). Verificado:
  sha256 idéntico local vs VM. → FINDINGS B-11.
- **Estado:** OK · **Rollback:** N/A

## 14:47 — Repo apt persistente (`apt-repo-publish.sh --serve` corregido)
- **Entorno:** prod · **Tipo:** reversible
- **Objetivo:** que el repo sobreviva reboots de fb-build antes de que otros dependan de él.
- **Ejecutado:** script corregido subido a `/tmp` (el checkout `/opt/fluxbee` no se tocó) y
  `/tmp/apt-repo-publish.sh --deb /opt/fluxbee/dist/fluxbee_0.1.33_amd64.deb --serve`.
- **Resultado:** `/etc/systemd/system/fluxbee-apt.service`, `enabled`, `Transient=no`, Release 200.
  **Hallazgo durante la corrida:** el índice quedó en **0 paquetes durante los ~5,5 min** del
  re-hash (se regeneraba *in place*). → FINDINGS B-10.
- **Estado:** OK
- **Rollback:** `systemctl disable --now fluxbee-apt && rm /etc/systemd/system/fluxbee-apt.service`

## 14:53–15:16 — qga de fb-build trabado: diagnóstico y causa raíz
- **Entorno:** prod · **Tipo:** lectura + reversible (2 reboots, 3 resets de fb-build)
- **Síntoma:** después de un `exec` el agente dejó de responder (hasta el `ping`), y tras cada
  reboot/reset respondía el `ping` pero volvía a trabarse en el primer `exec`.
- **Hipótesis descartadas con evidencia:** carga (host cpu 0 %, VM idle); disco lleno (`/` al
  34 %, vía `agent/get-fsinfo`); kernel nuevo (el reboot sí activó **6.8.0-142**, pero `exec` de
  `true`/`uname`/`bash -lc` funcionan sobre él); caracteres no-ASCII en general (`é` pasa).
- **Causa raíz (A/B controlado):** `exec` con `—` (U+2014) → `guest-exec failed - got timeout`
  y el agente queda muerto hasta reiniciarse; con `é` (U+00E9, Latin-1) no pasa nada. Los `exec`
  que lo trabaron hoy tenían `—`. → FINDINGS B-11.
- **Fix:** `lab/pve.py` — argv no-ASCII viaja en base64 y un bootstrap ASCII lo decodifica en el
  guest; además se deshace el mojibake de la salida y se define `HOME` (solución de B-7, que no se
  había aplicado). Validado en vivo: acentos/`—`/`✓`, exit codes, y el agente sobrevive.
- **Observación:** todas las VMs de PROD tienen reboot pendiente (kernels 6.8.0-137…142 + libc6,
  instalados por `unattended-upgrades`). fb-build ya corre 142 sin problemas. → FINDINGS B-12.
- **Estado:** OK · **Rollback:** N/A

## 15:17 — Placa `net1` en `vmbr0` para fb-build
- **Entorno:** prod · **Tipo:** reversible (placa de VM sobre un bridge existente; no toca la red del host)
- **Ejecutado:** `POST /nodes/pve/qemu/110/config net1=virtio,bridge=vmbr0` (patrón del par fb-egress).
- **Resultado:** `net1 = virtio=BC:24:11:EF:96:80,bridge=vmbr0`, hotplug (sin reboot, sin pendientes);
  el guest la ve como `ens19`. `eth0` intacta.
- **Estado:** OK · **Rollback:** `POST .../config delete=net1`

## 15:18 — Red de `ens19`: DHCP sin ruta por defecto + policy routing
- **Entorno:** prod · **Tipo:** reversible
- **Objetivo:** IP en la 8.x sin robarle la ruta por defecto (internet) a `fbint`, y que las
  respuestas a clientes de otras subredes (4.x por VPN) vuelvan por la 8.x.
- **Ejecutado:** `/etc/netplan/60-lan-vmbr0.yaml` (match por MAC; `dhcp4` con
  `use-routes/use-dns/use-domains: false`; `optional: true`; tabla 108 con default por
  192.168.8.1 + la 8.x on-link; regla `from 192.168.8.0/24 lookup 108` prio 100), `netplan generate`
  (valida) y `netplan apply`. No se tocó `50-cloud-init.yaml`.
- **Resultado:** `ens19` = **192.168.8.180/24** (lease: router 192.168.8.1, 2 h). Default de la
  tabla main sigue por `eth0` → 10.10.10.1; github 200.
- **Estado:** OK · **Rollback:** `rm /etc/netplan/60-lan-vmbr0.yaml && netplan apply`

## 15:19 — Verificación de acceso de un cliente (pregunta 1)
- **Entorno:** prod · **Tipo:** lectura
- **Ejecutado / Resultado:**
  - Mac del operador por VPN (origen `10.100.0.2`): `Release` 200, 33 versiones, `.deb` 0.1.33
    servido (~1,75 MB/s por la VPN). Como el origen está **fuera** de la 8.x, esto prueba también
    que el policy routing devuelve bien las respuestas.
  - fb-egress (Ubuntu 24.04 en la 8.x), con `apt` aislado en `/tmp` (sin tocar el sistema):
    `apt-get update` baja `Release` + `Packages` de 192.168.8.180; candidato **0.1.33**;
    `apt-get install -s fluxbee` → **13 nuevos** (`postgresql-16`, `postgresql`, `fluxbee`…).
- **Estado:** OK · **Rollback:** N/A

## 15:21 — Validación en vivo del publish atómico
- **Entorno:** prod · **Tipo:** reversible (re-publica el mismo 0.1.33)
- **Ejecutado:** script final (índice en `.new` + renames; `.deb` vía `.partial` + `mv`) y un
  publish real, contando versiones desde la Mac cada ~10 s.
- **Resultado:** 32 muestras en ~5 min, **mínimo 33** (antes: 0 durante toda la corrida). `EXIT=0`,
  sin temporales residuales. El one-liner imprime las dos IPs (10.10.10.50 y 192.168.8.180).
- **Estado:** OK · **Rollback:** N/A

---

## Decisiones del operador

1. **IP del repo: queda DHCP.** El router mantiene la asignación mientras la MAC no cambie
   (`BC:24:11:EF:96:80` → `192.168.8.180`); no se reserva ni se fija. *(Se aparta de la guía
   general de `docs/packaging-and-build.md` §4.3 por decisión explícita.)* Regla derivada: **no
   recrear `net1`** (otra MAC → otra IP). Anotado en el HANDBOOK.
2. **fb-build queda siempre encendida** (sirve el repo a los devs). HANDBOOK actualizado.
3. **Commit, con convención nueva:** todo commit referencia la versión actual de fluxbee en el
   asunto (`[0.1.33] …`), el cuerpo referencia los cambios, y todo install cambia la versión.
   No estaba escrito → agregado al HANDBOOK §12 ("Commits y versiones").
4. **Sin firma GPG** del repo: sigue `[trusted=yes]`.
5. **La infra la maneja el agente** ("hacé lo que creas conveniente — no hay nadie usando PROD,
   estamos en piloto alfa") → se aplican los reboots pendientes de B-12 (entradas siguientes).
