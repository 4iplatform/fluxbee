# Bitácora — 2026-07-28

**Entorno del día:** PROD `192.168.8.207` (nodo Proxmox `pve`) — primer contacto.
**Método:** [`METHOD.md`](METHOD.md). Horas en **ART (−03)**; el host corre `America/New_York` (EDT, −04),
o sea **hora bitácora = hora host + 1** para correlacionar con `journalctl` del host.

**Resumen del día:** reconocimiento del server de producción y verificación empírica de capacidades.
**No se hizo ningún cambio en prod** — todo lectura. Se acordó arrancar por el método + bitácora
antes de tocar infra.

---

## ~12:20 — Primer acceso al Proxmox de prod + inventario

- **Entorno:** prod (192.168.8.207)
- **Tipo:** lectura
- **Objetivo:** confirmar acceso con el token nuevo y saber qué hay en el server.
- **Ejecutado:**
  ```bash
  PVE_HOST=192.168.8.207 PVE_TOKEN='ai-agent@pve!vscode=<token>' python3 lab/pve.py nodes
  PVE_NODE=pve python3 lab/pve.py list
  curl -sk -H "Authorization: PVEAPIToken=..." https://192.168.8.207:8006/api2/json/nodes/pve/{storage,network,status,dns}
  ```
- **Resultado:**
  - Nodo `pve` online. **Proxmox 9.2.2**, kernel `7.0.2-6-pve`.
  - CPU: **Xeon E5-2620 @2.00GHz, 8 cores**.
  - **VMs: cero. Templates: cero. ISOs: cero.** Server virgen.
  - Storage: `local` (dir) **80.1 GB libres / 89 GB** (iso, vztmpl, backup, import) ·
    `local-lvm` (lvmthin) **200.2 GB libres / 200.2 GB** (images, rootdir).
  - Red: bridge **`vmbr0` = 192.168.8.207/24, gw 192.168.8.1**, puerto `nic0`.
    **`nic1` existe y está SIN configurar** (candidato a pata pública/DMZ).
  - DNS: `192.168.8.1` · **search domain `cloud.4iplatform.com`**.
- **Estado:** OK
- **Rollback:** N/A (lectura)

---

## ~12:28 — RAM del host ampliada a 24 GB (cambio hecho por el operador)

- **Entorno:** prod
- **Tipo:** cambio de hardware (**ejecutado por el operador**, no por el agente)
- **Objetivo:** dar aire para MB + worker + ingress (+ egress).
- **Resultado:** memoria del host **16.8 GB → 25.2 GB** totales (1.8 GB en uso).
  Deja **~23 GB** utilizables para VMs.
- **Estado:** OK (verificado vía `/nodes/pve/status`)
- **Rollback:** N/A (hardware)

---

## 12:28 — Verificación empírica de capacidades del token

- **Entorno:** prod
- **Tipo:** lectura
- **Objetivo:** saber con precisión **qué puede y qué no** el agente en este datacenter, sin asumir.
- **Ejecutado:**
  ```bash
  GET /api2/json/access/permissions      # permisos efectivos
  GET /api2/json/access/acl              # ACL configurado
  GET /api2/json/access/users/ai-agent@pve/token/vscode
  ```
- **Resultado:**
  - ACL: **`/` → `ai-agent@pve` role=`Administrator` propagate=1**
  - Token `vscode`: **`privsep=0`** (hereda todos los privilegios del usuario).
  - Privilegios efectivos en `/` (y propagados a `/nodes`, `/storage`, `/vms`, `/sdn`, `/pool`, `/access`):
    `VM.Allocate/Clone/Config.*/PowerMgmt/Snapshot/Migrate`, `VM.GuestAgent.Unrestricted`,
    `Datastore.Allocate/AllocateSpace/AllocateTemplate`, `Sys.Modify/PowerMgmt/Console/Syslog/AccessNetwork`,
    `SDN.Allocate`, `Permissions.Modify`, `User.Modify`, `Realm.Allocate`, `Pool.Allocate`, `Mapping.Modify`.
  - **Conclusión:** admin total del datacenter vía API. Único límite real: **no hay endpoint REST para
    ejecutar comandos arbitrarios sobre el host Proxmox** (sí sobre las VMs, vía guest-agent).
    Detalle en `METHOD.md` §2.
- **Estado:** OK
- **Rollback:** N/A (lectura)

---

## 12:35 — Creación del logbook (método + bitácora)

- **Entorno:** repo (no toca infra)
- **Tipo:** reversible
- **Objetivo:** registro serio de todo cambio en prod, y método acordado de trabajo.
- **Ejecutado:** alta de `lab/logbook/METHOD.md` y `lab/logbook/2026-07-28.md`.
- **Estado:** OK
- **Rollback:** `git revert` del commit.

---

## 12:45 — Verificación de la topología de red (doble red)

- **Entorno:** prod
- **Tipo:** lectura
- **Objetivo:** confirmar la red doble descrita por el operador (admin 8.x DHCP + segunda placa a
  router con IPs fijas públicas) antes de diseñar el ingress.
- **Ejecutado:**
  ```bash
  GET /api2/json/nodes/pve/network              # config completa
  GET /api2/json/nodes/pve/network/{nic0,nic1,vmbr0}
  GET /api2/json/cluster/status
  ```
- **Resultado:**
  - **`vmbr0`** — `active=1`, `192.168.8.207/24`, gw `192.168.8.1`, `bridge_ports=nic0`,
    `method=static` (el host tiene IP fija aunque la red sea DHCP: normal para el hipervisor).
  - **`nic0`** — `active=1`, `exists=1`, altnames `enp11s0` / `enx000c292761aa`.
  - **`nic1`** — `exists=1` pero **SIN `active`** → la placa existe pero está **caída y sin configurar**.
    altnames `enp19s0` / `enx000c292761b4`. **No hay `vmbr1`.**
  - **MAC OUI `00:0c:29` en ambas placas → VMware, Inc.** ⇒ **este Proxmox corre como VM sobre VMware**;
    las dos NICs son virtuales.
  - Cluster: nodo único `pve`.
- **Hallazgos / riesgos:**
  1. **La segunda red NO está cableada a nivel Proxmox** — falta crear `vmbr1` sobre `nic1`.
  2. **Capa VMware por debajo**: a qué router llega `nic1` lo define el *port group* de VMware,
     invisible desde Proxmox. **Riesgo de virtualización anidada**: KVM dentro de VMware requiere
     *"expose hardware assisted virtualization"*; si está apagado las VMs no arrancan o van lentísimas.
     → **Verificar antes de construir el template** (ya nos mordió en el lab).
  3. **No se puede verificar el link/carrier de `nic1` desde la API** (muestra configuración, no estado
     de enlace) — límite Nivel-3 de `METHOD.md` §2. Destrabe: dato del port group VMware,
     `ip -br link show enp19s0` en el host, o crear `vmbr1` y probar (**cambio de red del host =
     irreversible → requiere aprobación**).
  4. **Direccionamiento**: la admin es DHCP, pero `add_hive` registra la dirección de cada spoke; si el
     lease cambia, el registro queda viejo. → Propuesta: **reserva DHCP por MAC** en el router 8.1.
- **Estado:** OK (lectura; **ningún cambio aplicado**)
- **Rollback:** N/A

---

## 12:50 — Regla de alcance de acceso incorporada al método

- **Entorno:** repo
- **Tipo:** reversible
- **Objetivo:** dejar asentada la restricción del operador sobre la red compartida.
- **Ejecutado:** `METHOD.md` §3 → nueva regla **0** (prod): el agente opera **solo sobre las máquinas
  que él mismo creó** + el host Proxmox. **No se escanea `192.168.8.0/24`** ni se accede a IPs de terceros.
- **Estado:** OK
- **Rollback:** `git revert`

---

## 13:10 — Nested virt: BLOQUEANTE detectado (evidencia)

- **Entorno:** prod · **Tipo:** lectura
- **Objetivo:** verificar si KVM funciona antes de invertir en el template.
- **Ejecutado:** `GET /api2/json/nodes/pve/status` → `cpuinfo`
- **Resultado:**
  - flags contienen **`hypervisor`** (confirma que corre virtualizado sobre VMware)
  - **NO contienen `vmx`** (Intel VT-x)
  - **`hvm: ""`** (vacío) ⇒ **sin virtualización por hardware**
  - topología: **8 sockets × 1 core** (subóptimo; conviene 1 socket × 8 cores)
- **Conclusión:** **KVM NO disponible.** Las VMs no arrancarán con `kvm=1`; con `kvm=0` sería
  emulación TCG (inviable para un hive con postgres + ~20 nodos).
- **Acción requerida (operador, en VMware, con la VM apagada):**
  - ESXi/vSphere: CPU → **"Expose hardware assisted virtualization to the guest OS"** (`vhv.enable="TRUE"`)
  - Workstation/Player: Processors → **"Virtualize Intel VT-x/EPT or AMD-V/RVI"**
- **Re-verificación:** misma consulta; debe aparecer `vmx` en flags y `hvm` no vacío.
- **Estado:** BLOQUEANTE ABIERTO · **Rollback:** N/A

---

## 13:20 — Red interna virtual 10.10.10.0/24 creada (SDN) ✅

- **Entorno:** prod · **Tipo:** **irreversible** (red del host) — **autorizado por el operador**
- **Objetivo:** sacar el tráfico interno de fluxbee de la red compartida `8.x` a una red propia,
  con el host Proxmox actuando de router/NAT.
- **Verificación previa:** SDN soportado (`libpve-network-perl 1.6.5`, `ifupdown2 3.3.0`, IPAM `pve`),
  sin zonas/vnets preexistentes.
- **Ejecutado:**
  ```bash
  POST /cluster/sdn/zones            type=simple zone=fluxint ipam=pve nodes=pve
  POST /cluster/sdn/vnets            vnet=fbint zone=fluxint
  POST /cluster/sdn/vnets/fbint/subnets  subnet=10.10.10.0/24 type=subnet gateway=10.10.10.1 snat=1
  PUT  /cluster/sdn                  # apply
  ```
- **Resultado:**
  - Tarea `UPID:pve:00029086:...:reloadnetworkall` → **exitstatus OK**
  - Zona `fluxint` → **status: available**
  - **`fbint` activo como bridge tipo `vnet`**, disponible para VMs
  - Subnet: `10.10.10.0/24`, gateway `10.10.10.1`, **`snat: 1`** ← el host hace de router/NAT
  - **`vmbr0` intacto**, sesión no se cortó, uptime sin cambios (no hubo reboot)
- **Pendiente de prueba funcional:** que una VM en `fbint` alcance `10.10.10.1` y salga a internet por
  NAT. **No se puede probar hasta resolver el bloqueante de nested virt** (no se puede bootear una VM).
- **Nota de diseño:** zona **Simple = node-local**. Si mañana hay un segundo host Proxmox, esta zona
  **no se extiende**: habría que migrar a **VXLAN/EVPN**. Anotado para cuando escale.
- **Efecto lateral positivo:** al ser red propia, **desaparece el problema de reservas DHCP** —
  las IPs internas se asignan estáticas por cloud-init y son estables para `add_hive`.
- **Estado:** OK (creado y aplicado; prueba funcional pendiente)
- **Rollback:** `DELETE /cluster/sdn/vnets/fbint/subnets/fluxint-10.10.10.0-24`,
  `DELETE /cluster/sdn/vnets/fbint`, `DELETE /cluster/sdn/zones/fluxint`, luego `PUT /cluster/sdn`.

---

## 13:40 — Nested virt HABILITADO + CPU corregida (operador) → bloqueante cerrado ✅

- **Entorno:** prod · **Tipo:** cambio de hardware (**ejecutado por el operador** en VMware)
- **Verificación (misma consulta que a las 13:10):**

| | antes | ahora |
|---|---|---|
| flag `vmx` | ausente | **presente** ✅ |
| flag `ept` | — | **presente** ✅ |
| `hvm` | `""` | **`"1"`** ✅ |
| topología CPU | 8 sockets × 1 core | **1 socket × 8 cores** ✅ |

  Host rebooteado (uptime 152 s). RAM 25.2 GB.
- **Efecto:** **KVM disponible** — ya se pueden bootear VMs con aceleración por hardware.
- **Verificación adicional — la red SDN sobrevivió el reboot:** `fbint` `active=1`,
  zona `fluxint` → `available`, `vmbr0` intacto. La config SDN **persiste** correctamente.
- **Estado:** OK — bloqueante del día CERRADO
- **Rollback:** N/A

---

## Estado al cierre

- **Prod `192.168.8.207`:** sin VMs, sin templates, sin ISOs.
- **Único cambio del agente en el día:** la red interna SDN (zona `fluxint` + vnet `fbint` +
  subnet `10.10.10.0/24` con SNAT). `vmbr0` intacto.
- Recursos: 8 cores · ~23 GB RAM · 200 GB lvmthin (thin) · 80 GB para imágenes.
- Acceso del agente: verificado, Administrator.

### Topología de red acordada

```text
vmbr0   nic0   192.168.8.0/24   admin compartida (DHCP, terceros)  → solo gestión del host
fbint   —      10.10.10.0/24    INTERNA fluxbee, SNAT por el host  → MB, worker, ingress(interna)
vmbr1   nic1   71.182.182.80    PÚBLICA (a crear)                  → ingress(externa) ÚNICAMENTE
```

Plan de direcciones internas (estáticas, red propia): MB `10.10.10.10` ·
worker `10.10.10.20` · ingress `10.10.10.30`.

## Pendientes / decisiones abiertas

1. 🔴 **BLOQUEANTE — nested virt apagado en VMware.** Sin esto no se puede bootear ninguna VM y
   toda la Capa 0 queda frenada. Acción del operador (ver entrada 13:10).
2. 🟠 **`vmbr1` (pública) sin crear**: falta saber de qué *port group* de VMware cuelga `nic1` y si
   tiene carrier. Además hacen falta **máscara y gateway** del bloque público de `71.182.182.80`.
3. 🟡 **Template**: construir sobre Ubuntu 24.04 cloud image. ⚠️ `lab/template-prep.sh` hornea
   `administrator/magicAI` → **para prod, variante sin password** (`ssh_access=key_only_persist`),
   `METHOD.md` §6. Recomendación: **un solo template** (la 2ª placa del ingress es config de clonado).
4. 🟡 **Egress: ¿sí o no?** Con la red interna + SNAT del host, el rol egress es aún menos necesario.
   → Decisión del operador.
5. 🟢 **`.deb` a prod**: se baja del build box del lab (192.168.4.200:8900) y se sube por guest-agent;
   las redes no se conectan entre sí.
6. 🟢 **Prueba funcional de la red interna** (VM en `fbint` → ping `10.10.10.1` → salida a internet):
   pendiente del punto 1.
