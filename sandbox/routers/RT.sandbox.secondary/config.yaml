# Configuración completa del router RT.sandbox.secondary

router:
  name: RT.sandbox.secondary
  island_id: sandbox

paths:
  state_dir: ./state
  node_socket_dir: /var/run/json-router/nodes
  shm_prefix: /jsr-

timers:
  hello_interval_ms: 10000
  dead_interval_ms: 40000
  heartbeat_interval_ms: 5000
  heartbeat_stale_ms: 30000

wan:
  listen: "127.0.0.1:9001"
  uplinks:
    - address: "127.0.0.1:9000"

local_peers:
  shm: []

routing:
  static_routes: []
