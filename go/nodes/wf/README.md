# WF Nodes

Fuentes de nodos y runtimes `WF.*`.

Convención actual:
- `common/`: código compartido de la familia WF.
- `wf-generic/`: runtime base ejecutable e instanciable.

En esta fase se deja creada la estructura, sin mover todavía código productivo.
